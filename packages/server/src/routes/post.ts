import express, { Request, Response } from "express";
import { Post } from "../models/post";

// Need to uncomment after changing some stuff
// import PostSchema from "../services/post-svc";

const router = express.Router();